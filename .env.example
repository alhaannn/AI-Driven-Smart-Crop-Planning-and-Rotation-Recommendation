# Django Configuration
SECRET_KEY=your-secret-key-here-generate-a-new-one
DEBUG=True

# Database Configuration (SQLite is default, configure if using PostgreSQL/MySQL)
# DATABASE_ENGINE=django.db.backends.sqlite3
# DATABASE_NAME=db.sqlite3
# DATABASE_USER=
# DATABASE_PASSWORD=
# DATABASE_HOST=
# DATABASE_PORT=

# Email Configuration (Optional - for password resets, notifications)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-email-password

# Weather API Configuration (Optional - if integrating weather services)
# WEATHER_API_KEY=your-weather-api-key-here
# WEATHER_API_URL=https://api.weatherapi.com/v1

# Security Settings
ALLOWED_HOSTS=localhost,127.0.0.1

# Other Settings
TIME_ZONE=UTC
LANGUAGE_CODE=en-us
